name: network
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.12
  - pip
  - pip:
      - networkx>=3.3
      - matplotlib>=3.9
      - numpy>=2.1
      - scikit-learn>=1.5
      - requests>=2.32
      - tqdm>=4.66
      - gradio>=4.44
      - socksio>=1.0
      - pytest>=8.3
